import os
import sys
"""
import requests
import json
import random
import psycopg2 as dbapi2
import psycopg2.extras
from random import shuffle
from passlib.hash import pbkdf2_sha256
"""

if __name__ == "__main__":
    print(
        os.getenv("asdfasd")
    )  ####when I write <export asdfasd=123> to console (while in venv without '<' '>') I can call it using os.getenv(key)
